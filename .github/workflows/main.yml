name: "Shared Master Workflow"

on:
  push:
    branches: ["master", "development", "development-project-configurator"]
  pull_request:
    branches: [ "master", "development", "development-project-configurator"]
  workflow_dispatch:

jobs:
  master-workflow:
    uses: EyeSeeTea/github-workflows/.github/workflows/master.yml@master
